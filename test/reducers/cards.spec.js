// need to test:
// adding a card with no inital type; do the backref entries appear
// adding a card with initial type; do the backref entries appear
// adding more than one card; do the ids appear correctly
// updating the card data; do the backref entries update
// removing a card in the middle
// adding a card that already exists; updates old card